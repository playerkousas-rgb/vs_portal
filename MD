# 82venture 速覽（詳細請睇 README.md）

深資童軍管理系統 · 純靜態 · 零依賴 · 棗紅主題 `#7B2233`

* 開機先**揀旅團或 MOCK**，揀完先出現登入畫面；閘面有「伺服器登記狀態／診斷／直接輸入編號」
  （Vercel 環境變數開嘅旅團睇唔到時自己查得到）
* **示範模式（MOCK）唔會自動記住**：橫額、頂部 bar、「更多」選單都有「離開示範」／「返 <你嘅旅團>」
* 登入以**個人**為單位（電郵／YMIS ＋ 密碼）—— 冇共用帳戶；入到去嘅權限＝名冊身份
  **團長**（每團一位，最高權限，可轉移）／**領袖** / **執委** / **團員**（另有隱藏維護身份，唔會顯示）
* 模組：儀表板（**現在結餘**＝期初＋收入−支出）、會議、財務（**雙財政年度**：AGM 旅年度 ＋ 4/1–3/31 童軍年度）、
  **用戶**（團長／領袖／執委／團員，可編輯身份；生日當月＋前 7 日提示）、物資（借用自動加減庫存、任何登入者都可批核）、
  團章（中英對照、可出 Word / PDF / QR、公開頁免登入）、**通告**（每次開一張，公開頁可報名）、
  **表格與同步**（欄位自訂、插入自己嘅 Sheet、Apps Script 寫入總 Sheet）、
  進度系統接駁、教學、帳號與系統
* **成員連結**（免登入，唔使帳戶）：`entry.html`（申報）、`borrow.html`（物資借用）、
  `notice.html`（通告＋回覆出席與否）、`constitution.html`（團章）—— APP 內「成員連結」頁有 QR／海報
* **防呆**：編輯內容先自動暫存喺瀏覽器（未撳「儲存」唔會寫入）、刪除要打字確認、可「還原」、
  總表同步只排隊唔會自動送出
* 真實資料（`data/units/<編號>/`）同示範資料（`data/mock/`）**完全分離**
* 多旅團：`data/units.json` 做註冊處，每個旅團一個資料夾；後端共用一條 Apps Script `/exec`
* 部署：Vercel（附 `vercel.json`）＋ Apps Script（喺 App 內「下載 Code.gs」）

```bash
python3 -m http.server 8000        # 開 http://localhost:8000/?u=0082
node tests/smoke.mjs real          # 測試（需先 npm install jsdom）
node tests/smoke.mjs mock
node tests/public.mjs              # 公開頁（團章／通告／記帳／物資借用）
node tests/gate.mjs                # 旅團選擇閘
```

| 網址 | 用途 |
|---|---|
| `/?u=0082` | 管理系統 App（個人身份登入：團長／領袖／執委／團員） |
| `/entry.html?u=0082` | 成員手機影相記一筆（免登入） |
| `/notice.html?u=0082&n=<通告編號>` | 通告公開頁＋報名（免登入） |
| `/borrow.html?u=0082` | 成員物資借用申請（免登入） |
| `/constitution.html?u=0082` | 團章公開頁（免登入） |

---

## 部署瘦身原則（Vercel）

> 一句話：**個站係根目錄靜態（`outputDirectory: "."`），所以根目錄入面乜都會變成部署產物。
> `.vercelignore` 係唯一一道閘。**

* **零執行期依賴**：`package.json` 冇 `dependencies`。`jsdom` 係 `devDependency`（只係測試用）。
  新增套件之前先問：可唔可以用 Node／瀏覽器內建做？可唔可以本地 vendor 一個細檔？
  （QR code 就係咁做：`assets/vendor/qrcode.js`，離線可用，唔使 CDN。）
* **建構工具一律入 `devDependencies`**，永遠唔好入 `dependencies`。
* **`vercel.json` 用 `npm ci --omit=dev`**：Vercel 上面**完全唔裝** `jsdom`
  （`api/*.js` 同 `scripts/*.mjs` 只用 Node 內建 ＋ 本地檔，實測確認）。
  所以 build 快咗，而且 `node_modules` 根本唔會存在於部署環境。
* **`.vercelignore` 排除咗**：`.git`、`node_modules`、`tests/`（550 KB）、`docs/`（225 KB）、
  `apps-script/`（生成物）、`README.md`、`MD`、`DEPLOY_GUIDE_FOR_TROOPS.md`、`dev-server.mjs`、
  `*.bak`／`*.tmp`／`*.old`／`*.log`／`*~`、`.env*`、`uploads/`。
* **千祈唔好 exclude**（運行時／建置必要）：`*.html`、`assets/**`、`data/**`
  （`units.json`、`mock/*` 示範模式、`progress/items.json`、build 生成嘅 `units.generated.json`）、
  `api/**`（Serverless ＋ `_registry.js` 同時被 build 讀）、`scripts/**`（`buildCommand`）、
  `package.json`／`package-lock.json`／`vercel.json`。
* **唔好 commit 生成物當原始碼**：`apps-script/Code.gs` 由 `gasTemplate()` 生成
  （`npm run build:gas`）。APP 內「下載 Code.gs」係喺瀏覽器即刻生成，**唔會** fetch 呢個檔。
* **唔好 commit 備份／暫存／日誌／截圖**：`*.bak`、`*.tmp`、`*.old`、`*.log`、`*~`、設計原稿。
  呢個 project **一個圖片檔都冇** —— UI 全部係 inline SVG（`lib/util.js` 嘅 `icon()`），
  所以先至咁細。加圖片之前先諗清楚係咪真係要。
* **改完 `.vercelignore` 一定要跑 `npm run lint`** —— 佢會逐個比對
  「HTML／JS 引用緊嘅檔」有冇被 exclude 到，仲會印出今次部署上傳幾多個檔、幾多 KB。

```bash
npm run lint     # 語法／JSON／HTML 引用／後端 action 白名單／.vercelignore 審計 ＋ 部署清單
npm run build    # 生成 Code.gs ＋ 焗旅團名單（同 Vercel buildCommand 一樣）
npm run check    # lint → build → test（上線前一定跑）
```

## 體積上限（死過返唔好話冇人提你）

| 限制 | 數 | 爆咗會點 |
|---|---|---|
| Vercel **回應**上限 | 4.5 MB | `loadDb` 回 500 純文字 → 前端讀唔到成份資料庫 |
| Vercel **請求**上限 | 4.5 MB | `saveDb`／`uploadPhotos` 送唔上去（`api/proxy.js` 自己限 4 MB 留水位） |
| Google Sheet 單格 | 50 000 字 | 所以「資料庫」分頁要分段存（`DB_CHUNK = 45000`） |
| 瀏覽器 localStorage | ~5 MB | 相片入 db 好快就爆 |

* **寫入**冇天花板：`saveDbPart`／`saveDbCommit` 分件上（每件 < 2.8 MB）。
* **讀取**冇天花板（v2.6.0 起）：`loadDbPart` 分段讀（每段 1 MB），前端 `pullDb` 自動用。
* **相片永遠唔好入 db JSON**：`uploadPhotos` 直上 Drive，db 只留連結。
  舊嘅 dataURL 相用「總表同步 → 體積檢查 → 相片瘦身」一次過清走。

## 登入＝一定要同後端核對過（2026-09-20 起）

* **閘面（揀旅團）** 淨係要 Vercel registry —— 呢個合理。
* **主控頁登入** 而家一定要後端答得到（`remote.requireBackendForLogin()`）。
  後端答唔到 → 登入頁出「**登入已封鎖**」，唔會入到去。
* 以前 `login()` 係**純本機比對**（零網絡請求），帳戶名單就算後端讀唔返都會有
  （`store.js` 一見 accounts 空就塞 `SEED_ACCOUNTS`）—— 所以「登入成功」
  從來只代表「呢部機有一份帳戶名單」。
* 登入頁會顯示「**帳戶來源**」：後端（N 個帳戶）／本機初始帳戶。
* ⚠️ 代價：後端壞咗／斷網 → 完全入唔到（包括睇返本機未存嘅資料）。
* ⚠️ 改呢塊嘢嗰陣，**千祈唔好用 `ensureFresh()` 做登入閘** ——
  佢喺「本機有未存改動」嗰陣會唔使問後端就回 `ok:true`（等於免檢）。

## 秘密一律放環境變數（唔好入 Git）

| 變數 | 用途 | 冇設會點 |
|---|---|---|
| `TROOP_<編號>_BACKEND` | 旅團嘅 Apps Script /exec | 呢個旅團經唔到代理 |
| `TROOP_<編號>_APIKEY` | 後端 API Key（留喺伺服器） | 讀寫被後端拒絕 |
| `SUPER_KEY` | **超管密碼（直接填明文）** | 超管登入關閉（fail closed） |
| `SUPER_USER` | 超管帳號名，預設 `sheep` | 用預設 |

改超管密碼 = 改 `SUPER_KEY` 個值 → Redeploy。就咁。

三條規則：

1. repo 入面只留「點樣核對」，唔留任何秘密（明文密碼一律唔入 Git）
2. 環境變數**喺請求嗰陣先讀**，唔好喺 module 頂層讀死
   （否則 Vercel 加咗變數，舊 instance 都仲用舊值）
3. **fail closed** —— 變數冇設就關閉成條路，唔好靜靜地放行

⚠️ 呢個做法保護到「密碼」，保護唔到「身份」—— 靜態網站嘅 `isSuper()` 淨係讀
localStorage，識開 DevTools 就改到。而家超管只 gate UI 所以冇實質損失；
將來超管要做真正敏感嘅嘢，嗰個操作要放喺伺服器端，自己再核對一次 `SUPER_KEY`。

### 秘密掃描（`tests/api.mjs`）

`npm run check` 會掃**成個 repo**（`.js/.mjs/.gs/.json/.md/.html`）搵：

* 寫死嘅後門密碼（形狀：密碼變數 `===` 純數字字面量）
* 舊嗰個超管 hash
* 測試 fixture 有冇用返真密碼

三條規矩：

1. **掃描範圍係成個 repo，唔係淨止 `assets/`** —— 秘密漏去 `tests/` 都係漏
2. **掃描器本身要有斷言驗佢認唔認得出後門** —— 一條永遠 pass 嘅掃描等於冇掃描
3. **加新掃描之後，臨時植入一個假後門跑一次**，確認佢真係紅燈先算數

⚠️ 剷走「當前版本」嘅秘密**唔等於**佢冇洩漏過 —— Git 歷史仲有。
   舊密碼如果喺其他地方都用緊，當佢已經洩漏，去改咗佢。
