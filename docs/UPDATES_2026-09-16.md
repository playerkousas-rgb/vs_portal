# 2026-09-16 修正記錄（團長 10 項）

> 測試：`npm test` → smoke real **492 通過 / 0 失敗** · smoke mock **454 / 0** ·
> public **73 / 0** · gate **19 / 0** · api **11 / 0** · progress **25 / 0**。

---

## 1–2. 進度系統：由「外連入口」改為**直接接駁**

**背景**：原本嘅做法係喺執委系統開一條帶身份嘅 VSBADGE 連結（`from=portal&ymis=…`）。
VSBADGE v3.1 已加咗伺服器端驗證（`api/portal.js`：要對得上旅團登記嘅 `portalOrigin`／`portalRoles`），
所以未登記就直接貼連結會出現 `referer_mismatch`。

**團長指示**：唔要外連，要喺執委系統內**直接讀 VSBADGE**，而且可以**直接勾進度**。

**實作**：

| 檔案 | 做咩 |
| --- | --- |
| `api/progress.js`（新） | 同源 POST 轉發層：`load`（讀）／`save`（勾／取消）／`saveOtherBadge`（其他獎章）／`items`（考核項目定義）。只准 `script.google.com/…/exec`（擋 open proxy）、payload ≤ 1 MB、上游 45 秒逾時、log 只記 metadata（唔記 API Key、唔記完整後端網址） |
| `api/_registry.js` | 新增 `getProgressRegistryEntry(id)`：`TROOP_<id>_PROGRESSBACKEND` / `_PROGRESSAPIKEY` / `_PROGRESSFRONT` / `_PROGRESSFRONTURL`（設咗就由伺服器端提供，前端可以完全唔填） |
| `assets/js/lib/progress.js`（新） | 前端客戶端：`progressCfg` / `setProgressCfg`（存喺 `profile.progress.backend`）、`loadRemote` / `loadItems` / `saveTicks` / `saveOtherBadges` / `testConnection`，同埋配對工具（`flattenItems`、`summarizeRemote`、`memberDetail`、`matchLocalMember`） |
| `assets/js/views/progress.js`（重寫） | 分頁：**總覽**（逐個獎章完成度）／**成員進度**（逐個人）／**勾選進度**（直接勾／取消、可全勾全清、可還原、填日期）／**設定**（貼 `/exec` ＋ API Key、測試連線、舊入口連結保留喺最底） |

**旅團自己設定**（唔使管理員改 Vercel 設定）：VSBADGE 選單「顯示 API Key」複製 API Key ＋
複製 Apps Script 網頁應用程式 `/exec` 網址 → 喺「進度 → 設定」填 → 測試連線。
**API Key＝執委身份**：有 Key 就可以讀同勾；寫入嘅係 VSBADGE 用緊嘅同一個 Google Sheet。

> 讀寫需要伺服器端：Vercel 部署自動有 `/api/progress`；本機用 `npm run dev`（純靜態伺服器冇 API，進度會停用）。

---

## 3. 改名「82venture · 深資童軍管理系統」→「深資童軍管理系統」

介面所有顯示名（旅團閘、側邊欄、主頁 hero、頁腳、查詢權限表、申請接入對話框、教學、README、部署指南）
都改為「深資童軍管理系統」。**Wire value 唔改**：`appType: '82venture'`、`source: '82venture'` 保持原樣，
以免同 VSBADGE 共用嘅收件匣同總表分頁對唔上。

## 4. 開新旅團申請：直接入 ADMIN 系統（同 VSBADGE 一條路）

* 旅團閘「部署指南」第 5 步寫明：申請表送出之後**直接落到平台管理員嘅 ADMIN 系統收件匣**
  （同 VSBADGE 共用同一個收件匣，用 `appType: '82venture'` 分辨），撳掣即開申請表。
* 送出改行同 VSBADGE 一樣嘅路：`POST /api/proxy` → `action:'submitRegistration'` →
  **伺服器端**轉發去收件匣（`SCOUT_ADMIN_API`，前端改唔到目的地）。
* **收件匣唔會回執**（團長指示：ADMIN 系統收到就 OK）——所以 POST 過得去就當送到，
  唔會因為回 HTML／空白而誤報失敗；只有連線／逾時，或者收件匣明確回 `{success:false}` 先當失敗，
  嗰陣 App 顯示「送唔到去 ADMIN 系統」＋ **複製申請內容**（WhatsApp／電郵畀管理員）／**再試一次**。
  成功畫面亦講明「呢個系統唔會回覆」，管理員收到會轉寄畀團長、開好團就 email 通知旅團。
* 伺服器路線失敗（或者純靜態部署冇 `/api`）→ 會**自動再直接送多一次**，寧願重複都唔好收唔到；
  呢種情況 UI 標明「未收到回執」＋列出伺服器路線嘅錯誤。
* Payload 加 `appName: '深資童軍管理系統'`（`appType` 保持 `82venture`，收件匣 filter 唔會斷）。

## 5. 領袖免收團費

`model.js` 新增 `feeExempt(member)`（`identity === 'leader'` 或 `member.feeExempt === true`）。
`feeGrid()` / `feeStats()` / `overdueFees()` / 首頁提示全部跳過免收嘅人；團費頁會列出「領袖免收團費」名單。
個人備註可以逐個開 `feeExempt`（例如榮譽會員）。

## 6. 財政年度報告：上年度結餘**唔再混入收入**

* 報告分四段：**上年度結餘（期初，唔計入收入）** → **本年度收入** → **本年度支出** → **本年度淨額**
* 期末結餘＝期初結餘 ＋ 收入 − 支出（每段獨立小計，唔會相加埋）
* 兩條數（旅年度 / 童軍年度）各自標明「上年度結餘（期初，唔計入收入）」
* Word／PDF 輸出同畫面一致

## 7. 帳目月份：冇紀錄嘅月份都揀得到

按月檢視嘅月份選擇器列出**全年 12 個月**，每項標示筆數或者「冇紀錄」（唔會再因為冇資料而消失）。

## 8. 新增「過往紀錄」分頁

「財務 → 過往紀錄」：**先揀年度 → 再揀月份**（12 個月＋全年），顯示期初結餘、該月收入／支出／淨額、
逐筆清單；可以一鍵跳返本年度同一個月。

## 9. 「帳目」分頁＝本年度

「帳目」只顯示本年度（依 `settings.fiscalYearStartMonth` 計），兩種檢視：
**總覽**（12 個月統計表，包括冇紀錄嘅月份）同**按月**（逐筆清單）。匯出／列印只覆蓋本年度。
上年度或更早嘅資料一律去「過往紀錄」。

## 10. 「表格」分頁移除，改為每頁自己改欄位

* 側邊欄**冇咗「表格」**；表頭亦冇咗相關按鈕
* 每個模組頁右上新增「**欄位**」掣：帳目（財務）／物資／借用／團員／通告／會議 ——
  改名、加、刪、改類型、必填、隱藏、排序、還原（同原本表格設計一樣，只係搬去該頁）
* **插入自己嘅 Sheet**、**總表同步**、**儲存用量／備份**搬去「**帳號與系統 → 資料管理（表格與同步（進階））**」
* 深層連結仍然有效（`#/tables/sync` 等），方便舊書籤

## 11. 通告（等緊附件）

團長會補上參考附件，未按新格式改。今次先做咗：通告頁／分享頁嘅「逐張通告統計」
（報名人數、出席、唔出席、未回覆、截止日）方便執委唔出實體通告都數得清。

---

## 其他一併處理

* 教學（`docs.js`）、`README.md`、`DEPLOY_GUIDE_FOR_TROOPS.md`、`docs/ADMIN_ONBOARDING.md`、
  `docs/MASTER_SHEET.md` 全部對齊新做法；`docs/PROGRESS_PORTAL_HANDOFF.md` 已移到 `docs/archive/`（舊 portal 設計停用）
* `dev-server.mjs`（新）：本機開發伺服器，同時服務靜態檔同 `api/*.js`（`npm run dev`）
* `tests/progress.mjs`（新）：25 項 API 測試（白名單、逾時、payload 上限、log 唔記 Key、伺服器端 env 優先）
* 申請接入對話框：講清楚「進度系統由旅團自己填 Script ＋ API Key（唔使外連）」
* 管理員 checklist（`onboard.js`）同步更新

---

## ⚠️ 設計更正（同日，團長指正）：唔係「接駁另一個系統」，係「一個後端、兩個前端」

原本嘅做法係「深資童軍管理系統去連 VSBADGE」，呢個講法係錯嘅 —— 唔需要連任何其他系統。

**正確模型**

```
                    ┌───────────────────────────────┐
   深資童軍管理系統 ───▶ │  旅團自己嘅後端（只有一個）      │ ◀─── 進度前端（團員／領袖用）
   （前端 ①）        │  Google Sheet ＋ Apps Script    │      （前端 ②）
                    └───────────────────────────────┘
```

* 進度資料本身就係寫入**旅團自己嘅後端**（同一張 Sheet、同一支 `/exec`）
* 深資童軍管理系統只做兩件事：**讀後端**（`?action=load`）／**寫後端**（`action=save`）
* **API Key＝執委身份**：對得上就讀得、勾得（Key 淨係去同源 `/api/progress`）
* 所以：冇外連、冇 portal、冇 `referer_mismatch`，亦**唔會提到任何其他系統名**

**具體改動**

| 之前 | 之後 |
| --- | --- |
| `api/progress.js` 有 `items` action（去讀 VSBADGE 前端嘅 `data/items.json`） | 刪除；改成可選 `catalog`（自訂考核項目定義，公開 https） |
| 考核項目靠對方前端網址 | **內建** `data/progress/items.json`（第 11 版綱要，離線可用） |
| `TROOP_<id>_PROGRESSFRONT` / `PROGRESSFRONTURL` | 刪除；改成 `TROOP_<id>_PROGRESSCATALOG` |
| 進度設定要填「前端網址」 | 唔使填：預設用返 Registry 登記嘅旅團後端（`backend.gasUrl` / `apiKey`），要覆蓋先填 |
| 設定頁有「外連模式（舊做法）」卡片＋ portal 相關函式 | 全部移除（`portalIdentity` / `readiness` / `checkConnection` / `PROGRESS_ROLES`） |
| 後端範本（`Code.gs`）只有 `sync` / `claim` / `noticeSignup` / `loan` | **加** `?action=load`、`action=save`、`action=saveOtherBadge`，並建立／維護 `進度追蹤`／`其他獎章`／`待批完成`／`活動履歷`／`待批履歷`／`成員名單` 分頁 —— 同一支 Script 餵兩個前端 |
| `apps-script/Code.gs` 同 `gastemplate.js` 要人手同步 | `npm run build:gas` 由範本產生（`npm test` 會自動跑，亦會檢查兩邊一致） |

分頁名同欄位順序**刻意同進度前端一致**（`進度追蹤`：YMIS／項目 ID／完成日期／更新時間／確認者／備註 …），
所以兩個前端寫入嘅係同一份資料，任何一邊勾都會即刻喺另一邊見到。

---

## 通告：報名連結 + QR Code + 一撳 WhatsApp 分享

（第 10 項嘅第一步；樣式／重點欄位等緊團長嘅附件再對齊）

* **分享對話框**（清單嘅「分享」或詳情頁「分享／QR」）：
  * **用 WhatsApp 分享** —— 直接開 `wa.me/?text=…`，標題／日期／地點／費用／名額／截止／內容頭 6 行＋報名連結全部填好；
    文字可以喺對話框改完先送（彈窗被封鎖就改用「複製 WhatsApp 文字」）
  * **QR Code** —— 畫面即時顯示；可以**儲存 QR 圖**（PNG，冇 canvas 就 GIF，方便貼落群組）＋ 下載 SVG ＋ 列印 A4 海報
  * **報名連結** —— `notice.html?u=<旅團>&n=<通告>`，免登入，附即時報名統計（已報名／出席／唔出席／未回覆）
* **詳情頁「分享報名（WhatsApp）」卡**：同上一樣嘅掣，加埋報名統計，一開頁就見到
* 報名照樣直接寫入旅團後端嘅 `報名` 分頁；「通告 → 所有報名」有逐張通告統計
* 相關：`util.js` 加 `qrDataUrl()` / `qrImg()`；`exporter.js` 加 `downloadQrImage()`（PNG／GIF）；
  `notices.js` 加 `shareText()` / `whatsappShareUrl()`（都會跟 JSON 備份走，冇額外依賴）

---

## 審批中心：批核都搬入深資童軍管理系統

**團長指示**：作為執委或領袖，可以直接在執委系統處理所有團內事務；成員就只專心自己的紀錄冊。

| 之前 | 之後 |
| --- | --- |
| 確認待批完成要喺進度前端（有勾選權嘅帳號）做 | 深資童軍管理系統「**進度記錄 → 審批中心**」直接**批准／拒絕** |
| 後端只有 `save` / `saveOtherBadge` 兩個寫入 action | **加** `reviewRequest`（待批完成）同 `reviewLogRequest`（待批履歷），一樣要 API Key |
| 批准只 append 一行（可能同現有紀錄重複） | 已有同一項目就**更新完成日期**，冇先開新行（兩個前端讀數唔會重複） |

* **批准**：寫入 `進度追蹤`（確認日期可以自己揀，唔填就用申報日期；備註會寫「由申請轉入：…」）
* **拒絕**：只改狀態做 `rejected` ＋ 記低審批人／時間／備註，**唔會刪紀錄**，團員可以重新申報
* **待批履歷**（團員自行申報嘅服務／活動紀錄）同一版處理：批准會寫入（或更新）`活動履歷`
* 冇勾選權限嘅帳號睇得到但批唔到；未建分頁會提示去執行 `initializeSheets`
* `/api/progress` 加 `reviewRequest` / `reviewLogRequest` 兩個 action；`lib/progress.js` 加對應函式
* 通告嘅 PDF／列印選項**照樣保留**（理想環保，現實仲有人想印）

---

## 通告欄位（參考真實通告得出）＋ 設定指示

### 1. 通告「活動詳情」欄位（清單驅動）
參考真實通告嘅資料組合，為通告加上常規欄位：
**活動日期、報名截止、活動地點、集合時間及地點、解散時間及地點、內容／程序、服裝、費用、名額、查詢**。

* 單一來源：`assets/js/lib/notice-fields.js`（`NOTICE_INFO_FIELDS`）—— 加一行，六個地方自動跟：
  編輯器表單、詳情／列印、公開頁 `notice.html`、WhatsApp 分享文字、總表「通告」分頁、表格欄位設計
* 空嘅欄位唔會顯示、唔會寫入（通告唔會有一堆空白行）
* 長文（須知、報名方法）照樣放「內容」正文，兩者唔會重複


### 2. 設定方式（寫成「點做」）
* 設好後端之後，想埋讀取進度追蹤：**「進度 → 設定」**貼 ① `/exec` 網址 ② API Key（Apps Script 執行 `showApiKey()`）→ 撳「測試連線」
* 兩個值存喺旅團自己嘅資料（跟 JSON 備份走）；想收埋條 Key 唔落前端先設 `TROOP_<id>_PROGRESS*`（可選）
* 文案同步：`views/progress.js` 設定頁、`views/docs.js` 教學、README、ADMIN_ONBOARDING、DEPLOY_GUIDE

### 3. 後端加鎖（同時多人寫入）
`Code.gs` 寫入路徑（勾進度／其他獎章／審批）加咗 `withLock()`（`LockService`）：
全團同時撳報名／勾進度會排隊，唔會撞到寫壞資料。配額細節見 [`GAS_QUOTA_AND_LIMITS.md`](GAS_QUOTA_AND_LIMITS.md)。

---

## 開新旅團：Vercel 環境變數就得（唔使改 Git）＋ 防呆＋通告由後端讀

### 1. 純環境變數開團
* `/api/units` 嘅旅團清單會合併到前端 Registry → 加咗 env 就喺旅團清單出現
* 支援 `TROOP_<編號>_NAME` / `_NAMEEN` / `_SHORT`；`_PROGRESSBACKEND` ＋ `_PROGRESSAPIKEY`
  齊全時，前端**唔使填任何嘢**就讀到進度（設定頁顯示「伺服器端已設定」）
* 呢類旅團冇 `data/units/<編號>/`：由**空白資料庫**開始（唔會再彈「讀唔到種子資料」警告）
* 安全修正：未登記嘅旅團**唔會**借用其他旅團／共用嘅後端（以前會 fallback 去共用 GAS）

### 2. 通告公開：由旅團自己後端讀（新通告唔使改 Git 都公開到）
* Code.gs 加 `通告全文` 分頁（一行一張通告嘅 JSON）＋ `action=notices`（只回 `published`）
* `api/proxy` 白名單加 `notices`；`notice.html` 讀唔到靜態通告檔時，會經同源 proxy 去旅團後端讀
* 報名：冇公開網址時自動經 `/api/proxy`（伺服器端解析後端＋Key），唔會送去其他旅團
* 公開頁必須 `status = published` 先見到（草稿安全）

### 3. 防呆：批核都要撳「確定」
* 審批中心「批准」同「拒絕」一樣要喺確認框撳「確定批准／確定拒絕」先寫入
* 確認框會列出**邊個、邊一項、確認日期**，畀你核對清楚

### 4. 「開新旅團」教學搬入 App（**只限超級管理員 `sheep` 登入先見到**）
* **教學 → 開新旅團（唔使改 Git）**（新章節 `#/docs/newunit`）：
  * 填旅團編號／名稱／`/exec` ／API Key → **即時產生 5 個 `TROOP_<編號>_*` 環境變數**，一撳複製
  * 「複製逐步指示」：Vercel Settings → Environment Variables → Redeploy → 驗收
  * 旅團登入之後嘅跟進（通告公開／進度／團章／名冊）＋ **檢查清單（照住剔）**
  * 方法 A（Git Registry）摘要 ＋ 指向 `docs/ADD_NEW_UNIT.md`、`docs/ADMIN_ONBOARDING.md`
* **帳號與系統 → 旅團設定**：加「開新旅團逐步教學」同「即刻產生環境變數」兩個掣（彈窗可直接複製）
* **旅團閘（未登入）**：加一行指路（管理員收到 `/exec` 之後去邊度睇教學）
* 產生器放喺 `assets/js/lib/onboard.js`：`envUnitTemplate()` / `envUnitSteps()`（教學頁同帳號頁共用，唔會兩邊走樣）
* **可見性**：呢章同相關按鈕只會喺 `sheep`（超級管理員）登入後出現 —— 因為只有超管改得到 Vercel。
  直接打 `#/docs/newunit` 嘅非超管只會見到「只限超級管理員」。
* **一般開新旅團教學唔喺登入後**：佢本來就喺<b>登入前旅團閘嘅「部署指南」</b>（毋須登入都睇得到）——
  5 步（下載 Code.gs → 貼落自己 Sheet → `initializeSheets` → 部署 Web App → 提交申請）；
  今次順手更新：分頁數目改為「全部（含進度追蹤／活動履歷等共用分頁）」、
  第 5 步講明管理員會加 `TROOP_<編號>_*`，同埋之後點樣開通告收報名。
* 所以登入後：<b>超管</b>見到環境變數設定教學；<b>其他人</b>登入後完全冇開新旅團相關內容 ——
  多旅團章節只講架構＋一句「部署指南喺登入前」；「旅團設定」頁對非超管亦唔再提開團。

