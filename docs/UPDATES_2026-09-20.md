# 2026-09-20 更新：只有一個儲存方式

> 團長原話：「太多地方會 SAVE，又互相蓋過。要淨係一個方式。」
>
> **定案（由今日起唯一嘅資料流）：**
>
> 1. **登入攞後端** —— 揀旅團（連資料庫）→ 讀帳戶 → 密碼登入。
>    登入嗰一刻攞到嘅資料 **＝ 後端嗰一刻嘅狀態**（呢份叫「基準」）。
> 2. **之後改乜都只係存喺瀏覽器**（`localStorage`），直至你自己撳「**儲存到後端**」。
>    冇自動儲存、冇背景讀、冇 focus／online／閂視窗補寫。
> 3. **撳儲存嗰陣**：
>    * 先問後端而家嘅版本；同我登入時攞嗰個唔同 ＝ **有人喺我之後儲存過** → 會話你知；
>    * 拉成份後端落嚟做 **三方比對**（基準 vs 我 vs 後端）：
>      * 對方改嘅格同我改嘅一樣 → 冇問題；
>      * 對方改嘅格同我改嘅唔同格（例如佢加張三、我加李四；佢點李小明、我點陳大文）→ **一齊儲存**；
>      * **同一格唔同值**（佢登記陳大文「遲到」、我登記陳大文「早走」）→ 逐格列出嚟，
>        話你知「呢一點同後端唔同，**未有**寫入」，要你再確認；
>      * 你剔咗「用我嘅」再確認 → 先至蓋過去（因為後改嗰個人可能真係改咗選項）。

---

## 邊度改咗

| 地方 | 以前 | 而家 |
|---|---|---|
| 開機／登入 | `syncBoot` 讀後端，但讀唔到照樣開機；`arm()` 見到 pending 就 2.5 秒後自動寫 | **等後端載入完先出登入頁**；讀唔到就出橫額（重試／照用）；登入前如果上次載入超過 60 秒會再攞一次；**唔會寫** |
| 改嘢之後 | 自動儲存（debounce）／會議模式 60 秒背景讀／切返視窗即拉即合併 | 淨係寫 `localStorage`，頂部顯示「**儲存到後端（N）**」 |
| 儲存 | 「立即儲存」／「立即同步」／「立即同步全部」／「同步到公開頁」／團章「發布」／「測試連線」…… **六七條路各自寫 db** | **一條路**：`saveToBackend()`（頂部掣、「總表同步 → 儲存到後端」、通告「同步到公開頁」、團章「發布」全部叫同一個） |
| 撞版 | 後端拒絕 → 自動「拉＋合併＋重存」（`mergeDbs`：物件欄位以後端為準，靜靜地蓋走你嘅） | **三方比對**（`merge3.js`）：唔撞嘅併埋寫；**撞嘅逐格問你**，唔確認就保留後端、唔寫 |
| 報表同步（`pushToMaster`） | 順便夾埋整個 db 寫「資料庫」分頁、清 pending | 只寫報表分頁（帳目／團員／通告…），**唔碰 db、唔清 pending** |
| 測試連線 | 順便 `pushToMaster` 寫報表 | 淨係讀（`status` ＋ `dbInfo`） |
| 空白／未載入過嘅裝置 | `blank_guard` 只擋「完全冇內容」嘅機 | **未由後端載入過（冇基準）一律唔准寫**（`reason: 'no_base'`） |
| 團員入口（`members.html`） | RSVP／交卷 `flush()` 即寫；60 秒 poll | 開機 `loadFromBackend`；交嘢 `saveToBackend({ policy: 'mine' })`（團員部機冇人答對話框，佢自己嗰格以佢為準）；**冇 poll** |
| 登出 | 直接返登入頁 | 有未存改動會問；登出後**再由後端攞一次**（下一個人登入嗰刻 ＝ 後端嗰刻） |

## 檔案

* `assets/js/lib/merge3.js` —— **新**：`diffDb`／`threeWay`／`overridesFor`／`describeConflict`
  （逐格 diff、id 陣列按 id 對位、`updatedAt` 之類軟欄位唔算衝突、`auditLog` 併集、人話描述）
* `assets/js/lib/remote.js` —— **重寫**：得返 `loadFromBackend`、`saveToBackend`、`discardAndReload`、`ensureFresh`
  ＋ 讀嘢用嘅 `pullDb`／`remoteInfo`／`testConnection`／`remoteDiagnose`／`uploadPhotos`／分件
  （`startPolling`／`startVisibilityWatch`／`checkRemote`／`flush`／`syncNow`／`arm`／`reconcile` 全部冇咗）
* `assets/js/lib/store.js` —— 基準快照 `venture82.unit.<編號>.base.v2`（`getBase`／`setBase`／`localChanges`）、
  `adoptRemote`／`setLocalMerged`／`commitSaved`／`applyChangesLocal`／`commitMeta`（連線設定只寫本機、唔計 pending）；
  `mergeDbs`／`objHash` 剷走；**pending 先加後寫 localStorage**（以前差一次，閂咗視窗再開會以為冇嘢未存）
* `assets/js/views/syncdialog.js` —— **新**：衝突對話框（每格一個剔：預設保留後端）、`saveWithDialog()`、`reloadFromBackend()`
* `assets/js/main.js` —— 開機等載入、登入頁橫額、頂部「儲存到後端（N）／重新載入」、登出重攞
* `assets/js/views/tables.js` —— 「總表同步」頁：儲存到後端／由後端重新載入／測試連線（淨係讀）／報表同步（唔碰 db）；「會議模式」開關剷走
* `assets/js/views/notices.js`、`constitution.js` —— 「同步到公開頁」／「發布」行 `saveWithDialog`
* `assets/js/public-hub.js` —— 團員入口照上表

## 後端（Code.gs）

**唔使更新**。仍然用 v2.5.0 嘅 `dbInfo`／`loadDb`／`saveDb { db, baseVersion }`（樂觀鎖）
＋ `saveDbPart`／`saveDbCommit`（分件）。前端寫上去嘅 `db` **唔會再夾帶 `sync`／`backend`**
（連線設定、API Key 唔應該經 Sheet 傳嚟傳去）。

## 測試

* `tests/merge3.mjs`（**新**，48 項）：diff／三方比對純函數契約 —— 同格同值、唔同格、同格唔同值、加／刪、`overridesFor`
* `tests/remote.mjs`（190 項）：
  * 「團長劇本」真 HTTP：A 建資料 → B 點「早走」未存走開 → C 點「遲到」＋李小明「出席」儲存 →
    B 返嚟撳儲存：**李小明入咗、陳大文嗰格列做衝突未寫**；唔確認 ＝ 後端「遲到」；確認「用我嘅」＝「早走」蓋過去；
    第三部機登入見到最終結果；同格同值冇衝突；上次未存就閂咗 → 再開機三方比對 → 揀「用我嘅」→ 儲存
  * 原始碼守門：`remote.js`／`main.js`／`public-hub.js` 冇 poll／visibility／flush／自動寫；報表同步唔夾 db；測試連線淨係讀
* `tests/syncorder.mjs`（44 項）：核對唔到後端版本 → 一個 byte 都唔寫；有人喺我登入後儲存過 → 三方比對兩邊都在；改完等幾耐都唔會自動寫
* `tests/smoke.mjs`：通告「同步到公開頁」＝ `dbInfo` → `saveDb` 同一條路；示範模式一個請求都唔送
* `npm test` 全鏈 **2,214 項 / 0 失敗**

## 對用家嘅意思

* 頂部見到「**儲存到後端（3）**」＝ 你有 3 次改動仲未寫入；撳佢先寫。
* 撳完如果彈「有人喺你之後儲存過」＋一張清單 —— 清單上嘅格係**兩邊都改咗但唔一樣**，
  未寫入；你要逐格決定：唔剔 ＝ 用後端嘅，剔咗 ＝ 用你嘅。**其他唔撞嘅改動已經一齊寫咗入去。**
* 「重新載入」＝ 丟棄本機未存嘅改動，由後端重新攞（會先問你）。
* 示範模式（MOCK）永遠唔寫後端。

---

# 同日追加（下午）：v2.6.0 分段讀取 ＋ 部署瘦身

> 團長原話：「但為什麼我無痕和普通視窗還是同步不到，更過份的是我登入帳戶後
> 老說我沒有後端，我沒有後端如何登入」＋「另外我需要瘦身」

## 一、真正死因（三個環，一個都唔可以少）

### 環 ①：單據相片「直上 Drive」**由頭到尾都未行得通**

`assets/js/views/finance.js` 嘅設計係啱嘅：

```js
if (remote.remoteConfigured?.()) {
  const up = await remote.uploadPhotos(photos, { id: cid });
  if (up?.ok) { photos = photos.map(…link…); photosOnDrive = true; }
}
…
add('claims', { id: cid, ...r, photos, photosOnDrive, … });   // ← 失敗就照舊入 db
```

但 `api/proxy.js` 嘅 `ALLOWED_ACTIONS` **漏咗 `uploadPhotos`**：

```js
// 事發時嘅白名單（冇 uploadPhotos）
'saveDb', 'loadDb', 'dbInfo', 'verifySetupKey', 'saveDbPart', 'saveDbCommit',
```

代理一见到唔喺白名單就即刻回 `400 {"error":"不支援的操作"}`，
`up.ok` 永遠係 `false` → `photos` 保留原裝 base64 `dataUrl` → **直寫入
`db.claims[].photos[].dataUrl`**。base64 仲會脹大約 33%。

> **所以團長問「單據係直上 Drive 嘅設計，點解會大過 4.5MB」——
> 因為條 Drive 路喺正式站上面一次都未行通。**
> 每一張喺 APP 內上傳嘅單據相，都以 base64 存咗入資料庫。
> 呢個唔係「單據太大」，係「單據根本冇上到 Drive」。

順帶：Code.gs 由 v2.3.0 起一直有 `uploadPhotos` 呢個 action（`tests/gas.mjs` 有測），
所以**後端冇錯，錯喺代理白名單**。呢類「前後端契約漏咗一格」嘅錯，
測試點解捉唔到？因為測試全部直打假 GAS，冇一個經 `api/proxy.js` 嘅白名單。
→ 而家 `scripts/lint.mjs` 會把「前端經代理送嘅 action」同「代理白名單」逐個比對。

### 環 ②：資料庫大過 4.5MB → `loadDb` 回 500 純文字 → 前端誤判「冇 /api」

Vercel 代理**單一回應有 4.5MB 硬上限**。爆咗佢唔會回 JSON，而係回純文字
`FUNCTION_RESPONSE_PAYLOAD_TOO_LARGE`。

`lib/gateway.js` 舊嘅 `postJson` 見到「唔係 JSON」就只回一句
「後端回應格式異常（HTTP 500）」—— 用家完全估唔到死因。

> 更正一個流傳咗嘅講法：呢種情況**唔會**跌落自助路線變「未設定後端網址」。
> `postJson` 只在 `res.status === 404` 或者回應係 HTML 嗰陣先至 set `noApi:true`，
> 而 500 純文字兩樣都唔係 → `noApi:false` → `postBackend` 直接 return，唔會行 ②。
> 真正會講「未設定後端網址」嘅係另一條路（`/api/proxy` 真的 404／回 HTML）。
> 不過兩條路都係「把真正死因蓋住」，所以今次一齊修。

### 環 ③：每部機讀唔到後端 → 各自儲存自己嗰份 → 兩邊永遠對唔到料

2026-09-20 朝早改咗做「等後端載入完先出登入頁」之後，
呢個一直存在嘅故障終於浮面（以前讀失敗係靜靜地當冇事）。
**登入成功唔代表後端連到** —— 帳戶名單本身有本機種子。

## 二、修咗乜

| 檔 | 改動 |
|---|---|
| `api/proxy.js` | ★ 白名單加 `uploadPhotos`（環 ① 嘅正主）＋ `loadDbPart`；刪咗由頭到尾冇被讀過嘅死碼 `BIG_BODY_ACTIONS` |
| `assets/js/lib/gastemplate.js` → `apps-script/Code.gs` **v2.6.0** | 新 action `loadDbPart`（每段 1,000,000 字，每段帶 version）；抽出 `dbRawText()` 做 `loadDb`／`loadDbPart` 共用嘅**唯一**讀法；`SUPPORTED_ACTIONS` 改為陣列（以前手寫死嘅字串漏咗 `constitution`） |
| `assets/js/lib/remote.js` | `pullDbSegmented()`（逐段拼、version 撞咗就由頭再讀、段號超範圍明確報錯）；`pullDb({ bytes })` 已知太大就直接分段，否則單一讀失敗自動退去分段；`remoteDiagnose()` 加「整份資料庫讀取」一格（講體積、有冇超 4.5MB、有冇行分段） |
| `assets/js/lib/gateway.js` | `PAYLOAD_TOO_LARGE_RE` ＋ `tooLarge` 欄；回應過大 → `reason:'too_large'`，**一定唔會**跌落自助路線變「未設定後端網址」；錯誤訊息直接講 4.5MB 同分段讀 |
| `dev-server.mjs` | 本機模擬 Vercel 4.5MB 回應上限（爆咗回 500 純文字，同 Vercel 一模一樣） |
| `scripts/lint.mjs`（新） | 零依賴檢查：語法／JSON／HTML 引用／**代理白名單 vs 前端實送 action**／`SUPPORTED_ACTIONS` 一致性／`.vercelignore` 審計 ＋ 部署清單 |
| `package.json` | 加 `lint`／`build`／`check`（之前**一個都冇**，`npm run check` 係跑唔到嘅） |

### 點解唔係「先問體積先決定」

`dbInfo` 喺後端內部會行一次 `loadDb`（讀晒成份）。每次開機都先問一次
＝ 白白多燒一倍 GAS 配額（見 `docs/GAS_QUOTA_AND_LIMITS.md`）。
所以 `pullDb()` 預設**先試單一讀，失敗先退去分段**；
只有呼叫者已經知道體積（`pullDb({ bytes })`）先至直接分段。

## 三、測試

* `tests/gas.mjs`：92 → **114**（+22）。真係行生成出嚟嘅 `Code.gs`：
  5MB 資料庫 → 分件寫入 → `loadDbPart` 逐段讀返 → 拼埋 parse 得到、
  同成份 `loadDb` 同一個 version／bytes；段號超範圍明確報錯；負數 partIdx 唔擲錯；
  空後端 `found:false`；分段讀都有旅團隔離；冇 key → 未授權；
  未知 action 提示列出 `loadDbPart` 同 `constitution`。
* `tests/remote.mjs`：190 → **229**（+39）。代理放行 `uploadPhotos`／`loadDbPart`
  （而白名單以外照樣擋住）；5MB → 分段讀返；`loadDb` 撞 500 純文字 → 自動退去分段；
  讀緊嗰陣撞正有人儲存 → 由頭再讀；舊版後端 → 提示更新 Apps Script（唔係「未設定後端網址」）；
  `reason:'too_large'`；「同步診斷」有「整份資料庫讀取」一格。
* 順手修好一個**脆弱斷言**：`doPost 有處理 saveDb / loadDb / dbInfo` 原本係
  比對成句字面，加一個 action 就假紅燈 → 改成逐個 action 驗。
* **`npm test`：1,976 → 2,031 通過 / 0 失敗**（基線係今次 session 開始時實測嘅 1,976）。

## 四、部署瘦身

| 項目 | 之前 | 而家 |
|---|---|---|
| `.vercelignore` | **冇** | 有（見 `MD` 「部署瘦身原則」） |
| 部署上傳 | 125 個檔 · 2,372 KB | **73 個檔 · 1,492 KB** |
| `npm ci` on Vercel | 裝埋 `jsdom`（16 MB `node_modules`） | `--omit=dev`，**零套件** |
| 死重檔 | 根目錄有個名叫 `\|` 嘅檔（1,879 B，內容係一次 grep 輸出，誤入咗 repo） | 已 `git rm` |
| `npm run check/lint/build` | 三個都唔存在 | 三個都有 |

排除咗嘅：`tests/`（550 KB）、`docs/`（225 KB）、`apps-script/`（78 KB，生成物）、
`README.md`（40 KB）、`DEPLOY_GUIDE_FOR_TROOPS.md`（14 KB）、`dev-server.mjs`（8 KB）、`MD`（3 KB）。

**注意**：呢個 repo 本身得 2.3 MB，而且**一個圖片檔都冇**
（UI 全部 inline SVG）—— 所以「Vercel 儲存空間爆滿」唔會係靜態產物引起嘅。
真正食空間嘅係 **Google Sheet「資料庫」分頁 ＋ Drive 入面嘅單據相**，
而唔係呢個 deployment。

## 五、⚠️ 你要做嘅一件事

**合併之後要更新 Apps Script（約 5 分鐘，`/exec` 網址唔會變）：**

1. APP →「帳號與系統 → 資料管理 → 總表同步 → 後端 Apps Script 範本」→ 撳「下載 Code.gs」
2. 開你張 Google Sheet →「擴充功能 → Apps Script」→ 全選貼上 → 儲存
3. 「部署 → 管理部署作業 → 編輯（鉛筆）→ 版本：**新版本** → 部署」
4. 返 APP 撳「測試連線」→ 應該見到「後端 **v2.6.0**」
5. 再開個無痕視窗 → 登入頁應該冇咗紅色橫額，兩邊視窗就會對到料

**未更新 Apps Script 之前**，新版**唔會再呃你「冇後端」**：
佢會如實話「後端仲行緊 v2.5.0 之前嘅版本，未支援分段讀取（loadDbPart）」，
並且喺「同步診斷 → 整份資料庫讀取」嗰格話你知資料庫幾大、有冇超 4.5MB。

**之後建議做一次**：「總表同步 → 體積檢查 → 相片瘦身」，
把歷年積落嚟嘅單據相 dataURL 清走（有 Drive 連結嘅完全冇損失）。

---

# 同日追加（深夜）：登入硬閘 ＋ 超管密碼搬上環境變數

> 團長原話：「由首頁登入旅團嗰頁，入到係正常嘅，因為喺 Vercel 登記咗；
> 但能登入旅團內嘅主控頁就唔應該，因為嗰個係應該要帳戶同密碼同後端對上先至能進。
> 那既然都同後端對咗帳戶密碼，怎可能進去後會說沒連上後端，那剛才是登入那?」

**佢講得啱，而且呢個係設計錯，唔係一行 bug。**

## 一、「咁啱先係登入咗乜？」—— 你登入咗「你自己部機」

`assets/js/lib/auth.js` 嘅 `login()` 由頭到尾**冇一次網絡請求**
（369–437 行 grep `fetch|postBackend|callBackend|XMLHttp`：零命中）。
佢只做：喺 `localStorage` 嘅 `accounts[]` 搵 username → 本機 SHA-256 比對。

而 `accounts[]` 就算後端一個字都讀唔返都會有：

* `store.js:36-48` `SEED_ACCOUNTS` —— `leader`／`exco`，寫死喺 JS，`defaultPw: true`
* `store.js:341-344` —— 開機時 `if (!accounts.length) accounts = SEED_ACCOUNTS`
* `main.js:122-166` `syncBoot()` —— 讀後端失敗只係 `bootSyncWarn = r` 然後畫個橫額，**冇擋住 `renderLogin()`**

所以「登入成功」只代表「呢部機有一份帳戶名單」，同後端零關係。

## 二、改咗乜：登入硬閘

| 檔 | 改動 |
|---|---|
| `lib/remote.js` | 新 `requireBackendForLogin()` —— **一定要真係聯絡到後端**先至回 `ok:true`。回 `{version, at, empty, accounts}` 畀界面顯示 |
| `main.js` | `freshenBeforeLogin()`（「連唔到都照登入」）→ `gateLoginOnBackend()`；執委／領袖表單 ＋ 團員電郵登入三處一律先过硬閘，失敗即 `return` |
| `main.js` | 登入頁橫額由「連唔到旅團後端」改成「**登入已封鎖**」，並講明「唔係密碼錯」；另加「帳戶來源」一行（後端／本機種子），直接答「咁啱先係登入咗乜」 |
| `lib/remote.js` | `callBackend` 喺「兩條路都行唔到」嗰陣補返 `NO_ROUTE_HINT`（以前回空 hint，用家淨係見到「未設定後端網址」五個字） |

### ⚠️ 一個刻意唔踩嘅陷阱

`requireBackendForLogin()` **唔可以用 `ensureFresh()`**。`ensureFresh()` 有呢一行：

```js
if (hasPending()) return { ok: true, fresh: false, skipped: 'pending' };
```

即係「本機有未存改動」嗰陣佢會**完全唔聯絡後端**就回 `ok:true`。
攞佢做登入閘 ＝ 有未存改動嘅機照樣免檢登入 —— 正正係今次要修嘅嘢。
`tests/remote.mjs` 有一條斷言专门睇住呢點。

### 代價（你揀咗「硬擋」，呢個係佢嘅代價）

後端壞咗／斷網 → **完全入唔到**，包括睇返本機未存嘅資料。
如果你之後覺得太硬，可以改做「硬擋 ＋ 一個明示嘅唯讀緊急入口」。

## 三、超管密碼搬上環境變數（可以照抄去你其他系統）

### 之前有兩條後門（我第一輪只搵到一條）

```js
// auth.js 舊 code
const SUPER = { username: 'sheep', salt: 'v82:super:sheep', hash: '652debbf…' };
…
const ok = (await sha256Hex(SUPER.salt + '::' + p)) === SUPER.hash || p === '<四位數明文密碼>';
```

`tests/api.mjs` 加咗掃描之後，先至搵到**第二條**：

```js
export function isSuperCredential(username, password) {
  return … === SUPER.username && String(password) === '<四位數明文密碼>';
}
```

呢個函數**全 repo 一個 caller 都冇** —— 死碼兼後門。已刪。

### 教訓：掃描器本身都要被測試，掃描範圍要係成個 repo

搵秘密唔好靠肉眼。但寫咗掃描之後，我連續踩咗三個坑，三個都值得記低：

1. **第一版 regex 認唔到第二條後門。**
   直接搵真密碼字串嗰種寫法，認到 `p === <密碼>`，但如果後門寫成
   `String(password) === <密碼>`（多咗個收掣括號）就漏網。
   而家用「形狀」而唔係用真密碼：
   ```js
   const BACKDOOR_RE = /(?:\bp\b|\bpassword\b)\s*\)*\s*===\s*'\d{3,8}'/;
   ```
   `\)*` 係要吞埋 `String(password)` 嗰個收掣括號。
   而且**加咗兩條斷言驗呢條 regex 本身**（認得出後門、唔會誤傷
   `p === TEMP_PASSWORD`）—— 一條永遠 pass 嘅掃描等於冇掃描。

2. **只掃 `assets/` 唔夠。** 改嘢過程中，真密碼一度由 `auth.js` 搬咗去
   `tests/_authstub.mjs` 做測試 fixture —— 只掃 `assets/` 會綠燈通過，
   而密碼照樣留喺公開 repo。**秘密漏去邊個目錄都係漏**，而家掃成個 repo
   （126 個 `.js/.mjs/.gs/.json/.md/.html`，只豁免掃描器自己）。
   測試 fixture 亦已改用 `'test-super-pw-2026'`，唔再用真密碼。

3. **掃描器要驗到「真係捉得到」。** 我臨時喺 `auth.js` 尾植入
   一個「密碼變數直接等於某個四位數字面量」嘅假後門，跑測試 → 兩條斷言即刻紅燈
   並印出 `漏咗喺：assets/js/lib/auth.js`；還原之後回復 59/0。
   冇做過呢一步，你唔會知條掃描係咪由頭到尾都係綠燈擺設。

⚠️ 順帶一提：舊密碼仍然留喺 **Git 歷史**入面。呢次改動剷走嘅係「當前版本」。
   如果嗰條密碼你喺其他地方都用緊，**當佢已經洩漏，去改咗佢**。

### 而家嘅做法（`api/auth.js`）

Vercel → Settings → Environment Variables：

```
SUPER_KEY = 你嘅密碼
```

一個變數，直接填明文密碼，搞掂。改密碼 = 改個值 → Redeploy。
（選填 `SUPER_USER`，預設 `sheep`。）

```
瀏覽器 ──POST {user, password}──▶ /api/auth（Vercel Serverless）
                                      │  process.env.SUPER_KEY  ← 只喺伺服器
                                      │  crypto.timingSafeEqual()
                                      ◀─ {ok:true}
```

密碼永遠唔會落瀏覽器、唔會入 Git。

**三條規則（你其他系統照抄）：**

1. **repo 入面只留「點樣核對」，唔留任何秘密。**
2. **環境變數喺請求嗰陣先讀，唔好喺 module 頂層讀死。**
   ```js
   // ✗ 頂層讀死：Vercel 加咗變數，舊 instance 都仲用舊值
   const KEY = process.env.SUPER_KEY || '';
   // ✓ 每次 handler 行嗰陣先攞
   function cfg() { return { key: process.env.SUPER_KEY || '' }; }
   ```
   （我自己第一版就犯咗呢個錯，測試即刻紅燈。）
3. **Fail closed。** 變數冇設 → 回 `503 {disabled:true}`，成條路關閉。
   寧願自己一時入唔到，都好過留一條任何人都入到嘅門。

另外：用 `crypto.timingSafeEqual()` 而唔係 `===`（長度同首幾個字元會洩漏資訊），
而且 username 錯同密碼錯要回**同一句**訊息，否則人可以用嚟確認邊個帳號存在。

> 註：我第一版整咗 `SUPER_ADMIN_HASH` ＋ `SESSION_SECRET` ＋ HMAC token 簽發，
> 團長話「使唔使設定得咁複雜」—— 佢講得啱。呢個 App 嘅超管只 gate UI，
> 冇任何伺服器端特權操作，簽 token 係為咗一個唔存在嘅需求。已簡化成一個變數。

### ⚠️ 呢個做法保護到乜、保護唔到乜

* **保護到：密碼。** 冇人可以由公開 repo 讀到佢，亦冇人可以離線爆佢。
* **保護唔到：「超管」呢個身份本身。** 個 App 係靜態網站，`isSuper()` 淨係讀
  localStorage —— 識開 DevTools 嘅人可以人手寫 `{"role":"super"}` 落去。
  而家超管只 gate UI（`accounts.js` 嘅 MOCK tab、`docs.js` 嘅開新旅團教學、
  「開新旅團」掣），冇一個真正嘅伺服器端特權操作，所以暫時冇實質損失。
  **將來超管如果要做真正敏感嘅嘢（改 registry、清後端、讀人哋旅團），
  嗰個操作本身一定要放喺伺服器端，並且驗 `api/auth.js` 簽發嘅 token** ——
  嗰個操作本身一定要放喺伺服器端，並且自己再核對一次 `SUPER_KEY`。

## 四、測試

`npm run check` exit 0。**2,037 → 2,073 通過 / 0 失敗**（+36）；lint 14/0。

* `tests/api.mjs` 32→**51**：掃描**成個 repo**確保冇任何超管秘密返嚟
  （包括驗掃描器本身認唔認得出後門）；未設 `SUPER_KEY` → 503 fail closed；
  密碼啱 → 200；回應唔洩漏密碼；密碼錯／用戶名錯 → 同一句 401；
  改咗 `SUPER_KEY` → 舊密碼即刻入唔到、新密碼即刻入到；`SUPER_USER` 可以改帳號名。
* `tests/remote.mjs` 229→**246**：後端答唔到 → 唔准登入；
  **有未存改動都唔可以免檢放行**；唔會再講「未設定後端網址」；
  三處登入表單都接咗硬閘；舊 `freshenBeforeLogin` 已剷。
* `tests/_authstub.mjs`（新）：測試行嘅係**真** `api/auth.js` handler，唔係假嘢。
* 順手修好兩條自己嘅脆弱斷言（`|| p === TEMP_PASSWORD` 被誤當後門；
  歷史註解令「已剷走」斷言假紅燈 → 加 `stripComments()`）。

## 五、⚠️ 仲未解決：你普通視窗點解冇資料

硬閘係**防止**問題，唔係**修好**你而家個症狀。你講「無痕有資料、後端有資料、
普通視窗冇資料」—— 呢個仍然未解釋。两条已驗證嘅靜默失敗路徑：

* `store.js:70` `lsSet()` —— `localStorage.setItem` 失敗（quota 爆）會靜靜地
  跌入記憶體後備，關 tab 就冇晒，App 永遠唔會話你知
* `store.js:328` —— `if (parsed && parsed.schema === SCHEMA)`；JSON 壞咗或者
  schema 唔係 2 → 靜靜地掟走成份資料、重新種子（得一句 `console.warn`）

兩條都完美匹配「冇資料 ＋ 仍然登得到」。要確定係邊條，
請喺兩部機各跑一次之前畀你嘅 console 腳本，把輸出貼返嚟。

---

# 同日追加（接線狀態一致化）：Vercel 已接線就唔再要求第二套設定

今次修正針對以下矛盾：**登入前已經成功由 Vercel 代理讀到後端，但入到「總表同步」仍然顯示未設定；或者診斷把「未填本機 /exec」當成未有後端。**

## 正確關係

1. 揀旅團只係揀 `unit` 編號，本身唔等於後端已通；真正通唔通由 `/api/proxy` 回應決定。
2. 執委／領袖登入前會先 `loadFromBackend()`。登入成功代表今次已經由後端讀到資料庫（帳戶名單由後端資料提供；密碼再用該名單嘅 hash 核對）。
3. 如果旅團已在 Vercel 設定 `TROOP_<編號>_BACKEND` ＋ `TROOP_<編號>_APIKEY`，**總表同步唔需要再填 Apps Script `/exec` 或 API Key**。瀏覽器只送旅團編號，Vercel 伺服器注入真正後端資料。
4. 「同步設定」內嘅 `/exec`／API Key 改名為**自助後備設定**，只供純靜態部署（冇 `/api/proxy`）使用。

## 修正內容

* `assets/js/lib/remote.js` 新增 `backendStatus()`：記住今次實際成功嘅路線（`proxy`／`direct`），唔再只靠本機 `sync.url` 判斷有冇後端。
* Vercel 代理成功後，總表同步頁會顯示「已核對 · /api/proxy」；冇填本機 `/exec` 會被診斷為正確狀態，而唔係同步設定錯誤。
* 錯誤訊息分清「冇可用接線」同「代理已存在但暫時未能建立」；避免儲存／讀取流程反覆講「未有後端」。
* 無效、過期或錯誤格式嘅舊 `/exec` 設定唔再單靠「有字串」當成綠燈。
* `assets/js/views/tables.js` 的總表同步頁會沿用登入／載入實際驗證結果，並清楚標示 Vercel 代理係唯一正路。

# 同日再追加（普通用家簡化流程）：後端未通就停喺連線頁

按最新產品決定，真實旅團開機時必須先成功由同一條 Vercel Proxy 路線讀到後端，否則：

* `assets/js/main.js` 的 `syncBoot()` 現在所有分支都回傳 `{ ok: boolean }`；真實旅團若讀取失敗會清除舊 session，停喺「未能連接旅團後端」頁，不會顯示帳戶／密碼登入欄位。
* 連線頁只保留「再試一次」及「揀其他旅團」，普通用家只會看到「暫時未能連線，請稍後再試」，不需要輸入 `/exec`、API Key 或自行診斷。
* `assets/js/views/tables.js` 的普通同步流程只顯示連線／儲存狀態、「儲存到後端」及「由後端重新載入」。體積、診斷、搬遷及自助 `/exec`／API Key／Apps Script 範本／Payload 等工具只在非平台管理路線的管理員區域保留；Vercel Registry 正常時不顯示。
* `assets/js/views/progress.js` 在 Registry／Proxy 已提供接線時不再顯示 `/exec`、API Key、自訂目錄或設定分頁，改為只顯示已準備好的連線狀態及重新讀取。

以上沒有加入自動同步，也沒有新增第二條隱藏寫入路線；儲存及重新載入仍然由用家清楚按掣操作。

## 本輪驗證

* `npm run lint`：14/0。
* `npm run test:remote`：248/248。
* `npm test`：完整測試套件通過（包括 real／mock、Registry、同步、進度及入口流程）。

---

# 同日第三輪：團長三問（成員進度重複／同步生分身分頁／由後端重新載入）

團長回報三件事，逐件搵到死因，全部已修（後端範本升 **v2.6.1**，重新下載 `Code.gs` → 部署 → 版本揀「新版本」先去生效）：

## ①「成員進度不知為何重複了」

*   **死因**：報表同步（`writeTab`）用**前端 schema 欄位次序**寫「團員」分頁
    （`旅團,id,name,eng,identity,…,ymis,…`），但進度讀取（`progressMembers`）
    一路寫死「第 2 欄＝ymis、第 4 欄＝姓名」—— 嗰個係 `initializeSheets` 舊次序
    （`旅團,id,ymis,systemId,姓名,…`）。兩套次序唔同 → 讀錯欄：
    每位成員多咗一行「YMIS＝姓名、名＝身份」，望落就係「重複」。
*   **修正**：`progressMembers()` 改為照**標題列**搵返「ymis」同「姓名」喺邊一欄，
    兩個次序都啱；搵唔到先退返舊次序。
*   驗證：`tests/gas.mjs` 用**真 schema 次序**行一次同步 → 進度讀取 2 人（唔係 4 人）、
    YMIS 正確。**120/0 通過**。

## ②「同步後端會為這旅團再創建分頁（物資82）」

*   **死因**：`sheetName()` 喺 per-unit-sheet 模式回「分頁名·旅團編號」（`物資·0082`），
    同 `initializeSheets()` 建嘅無後綴分頁（`物資`）兩套名對唔上 → 每次同步都
    多生一張 `物資·0082`、`帳目·0082`……資料唔係填落原本嗰張。
*   **修正**：報表一律寫返**無後綴**分頁（同 `initializeSheets` 一致）；
    而且每次同步會順手刪走舊版留低嘅 `…·旅團編號` 分身（只刪**同一個旅團**後綴嗰啲）。
*   驗證：同步後只係 `物資`／`團員` 等，冇 `物資·0082`；模擬舊分身會被清走。**通過**。

## ③「由後端重新載入還是不成功」

*   **搵到嘅一個可修死因**：後端範本 `BACKEND_VERSION` 一直係 `'v2.5.0'`
    （明明已經寫咗 v2.6.0 功能，但版本號冇跟住升），令同步診斷／提示誤判後端版本。
    已升做 `'v2.6.1'`，並加測試鎖死「標頭版本＝BACKEND_VERSION」。
*   **其餘屬操作層**：程式碼鏈一路完整（連線 → dbInfo/loadDbPart 分段讀 → 授權 →
    採用資料庫），但要後端**真係部署咗新 Code.gs** 先會通：
    1. 「帳號與系統 → 資料管理 → 總表同步 → 後端 Apps Script 範本」撳「下載 Code.gs」
    2. 開旅團張 Sheet → 擴充功能 → Apps Script，**全部取代**貼上
    3. 儲存 → 部署 → **管理部署作業 → 編輯（鉛筆）→ 版本：新版本 → 部署**
       （`/exec` 網址不變，前端設定唔使改）
    「重新載入」若仍然失敗，喺「總表同步 → 同步診斷」逐格驗，邊格紅色就照嗰格提示做。

## 另外：全面按鈕審計（已清點，冇「壞掣」）

*   跑咗 66/66 組（mock/real × leader/exco/super × 11 分頁）、2811 個元素，2765 有反應。
*   審計標出嘅「死掣」全部查證係**誤報**：
    *   `列印海報`（links）—— 頭頂「列印團員 QR」同呢粒以前共用 `data-act="print-hub"`，
        `querySelector` 只綁到頭頂嗰粒，卡入面嗰粒真係冇反應 —— **已修**
        （改成獨立 `print-poster`）。
    *   33 個「無文字 topbar」＝ `#btnPw2`（改密碼）：`go('#/admin/data')` 如果
        「而家正正就喺嗰度」會冇肉眼變化，審計誤判為死掣 —— 實際正常。
    *   `公開頁預覽`＝ `window.open`（jsdom 唔支援）—— 正常。
    *   「printdoc」類別＝隱形列印 iframe 喺審計量度窗口（160ms）同清理（1500ms）
        撞埋一齊所致 —— 正常，已用獨立 jsdom 實測每一粒都真係開到列印框。

---

# v2.6.2（同日稍後）：「儲存唔到去後端／後端讀取唔到」嘅真正死因

> 團長原話：「都係唔得，我完全唔理解發生咩事，一直都係儲存唔到去後端／後端讀取唔到，
> 我只能用 JSON SAVE 但這完全不合理，能登入就是找到後端，不然應該是登入不了，
> 但登入又不能寫入又不能讀取，一定是中間有什麼擋了。」

## 佢嘅推理邊句啱、邊句唔啱

| 佢講 | 事實（呢次逐格查過） |
|---|---|
| 「能登入就是找到後端」 | **啱**。v2.6.0 起登入係硬閘：`main.js gateLoginOnBackend()` → `remote.requireBackendForLogin()` → `loadFromBackend()`，後端答唔到就**唔會顯示帳戶／密碼欄**（停喺「未能連接旅團後端」頁）。所以入到主控頁 ＝ 嗰一刻 `dbInfo`＋`loadDb` 真係成功過。 |
| 「一定是中間有什麼擋了」 | **啱，但唔係 Vercel**。實測 `https://ecportal.vercel.app/api/units?diag=1`：`TROOP_0082_BACKEND`／`TROOP_0082_APIKEY` 都認到（`trusted:["0082"], withKey:["0082"], env:production`）。擋住嘅係**最後一環：Apps Script 張 Sheet 本身**。 |
| 「只能 JSON SAVE」 | 因為讀寫一齊死，淨剩本機匯出／匯入行得通 —— 呢個正正係「後端讀寫都死」嘅症狀，唔係另一個問題。 |

## 死因（v2.6.1 之前嘅 `cleanStaging()`）

分件儲存（資料庫大過 2.8MB 自動行 `saveDbPart` × N → `saveDbCommit`）會把件暫時寫入
「資料庫」分頁嘅 `__staging__` 行，commit 嗰陣先清走。舊 `cleanStaging()` 係咁砌「一梳連續行」：

```js
if (run && run[0] === rowNo + 1) run[0] = rowNo;   // ← 把梳嘅起點向上移
else { if (run) sh.deleteRows(run[0], run[1]); run = [rowNo, 1]; }
```

起點移咗，但**梳長 `run[1]` 冇加**。所以一梳 N 行連續暫存行，最後只刪到 **1 行**。

後果（用同一支量度程式、同一份 3.25 MB 資料庫，分別餵畀新舊 Code.gs 實測）：

| | 舊 v2.6.1 | 新 v2.6.2 |
|---|---|---|
| 正式段行 | 76 | 76 |
| 存 **1** 次之後留低嘅垃圾行 | **75 行 ＝ 3.21 MB** | **0 行** |
| 存 **2** 次之後 | **150 行 ＝ 6.42 MB**（分頁 227 行） | **0 行**（分頁 77 行） |

即係：**每存一次大資料庫，就把成份資料庫複製一份留喺「資料庫」分頁**。
分頁越嚟越大 → 每次 `getDataRange().getValues()` 越來越慢 →
最後 `saveDb`／`loadDb` 撞 Apps Script 執行時間／記憶體上限 → **讀寫一齊死**。

而 `status`／`ping`／「測試連線」完全唔掂「資料庫」分頁，所以照樣話「後端正常」——
呢個就係「明明連到、但讀寫唔到，好似中間有嘢擋」嘅來源。

## 修咗乜

* `assets/js/lib/gastemplate.js`（→ `apps-script/Code.gs`，`npm run build:gas`）
  * **新** `deleteRowRuns(sh, rowNos)`：全後端**唯一**嘅刪行方法（計梳 → 由最底嗰梳刪起）。
    `saveDb`／`saveDbPart`（清過期暫存）／`cleanStaging` 全部改行佢，冇第二套砌梳邏輯。
  * `saveDbCommit`：以前係「先 `cleanStaging()`，再用**刪之前**讀返嘅行號刪舊段」——
    行號會因為第一輪刪除而移位。而家把暫存行＋舊段**一次過**由底往上刪晒，唔會指錯行。
  * **新** `cleanStaleStaging()`（逃生門）：一次過清走「資料庫」分頁所有 `__staging__` 行，
    正式資料（旅團編號嗰啲行）一行都唔掂。已經中招嘅 Sheet 貼新版之後執行一次即可。
  * `dbRawText()` 順手数 `stagingRows`／`stagingBytes` → `loadDb`／`dbInfo` 報上去
    （讀成份分頁本來就要做，零額外成本）。
  * `BACKEND_VERSION` → **v2.6.2**。
* `assets/js/lib/remote.js` —— 「同步診斷」新一格 `junk`：`dbInfo` 報到暫存垃圾行就會出黃色警告
  ＋教人「更新 Code.gs → 執行 `cleanStaleStaging()`」。
* `assets/js/views/tables.js` —— 「總表同步 → 後端 Apps Script 範本」卡加咗同一個救法說明。

## 團長要做嘅三步

1. 「帳號與系統 → 資料管理 → 總表同步 → 後端 Apps Script 範本」撳**「下載 Code.gs」**（v2.6.2）。
2. 開旅團張 Google Sheet →「擴充功能 → Apps Script」→ **全部取代**貼上 → 儲存 →
   「部署 → 管理部署作業 → 編輯（鉛筆）→ 版本：**新版本** → 部署」（`/exec` 網址唔變）。
3. 喺 Apps Script 編輯器上面個函數選單揀 **`cleanStaleStaging`** → 撳「執行」一次
   （清走舊版積落嘅垃圾行；正式資料唔會甩）。
4. 返 app 撳「同步診斷」：應該見到後端版本 **v2.6.2**、冇黃色「後端暫存垃圾行」警告，
   然後撳「儲存到後端」。

## 測試（點解以前測唔到）

以前**所有**端到端測試都打 `tests/_fakegas.mjs` —— 一份**重寫**嘅假後端。
假後端嘅分件儲存係另一套寫法（冇呢個 bug），所以 CI 全綠、真站照死。

* `tests/_gasvm.mjs`（**新**，由 `tests/gas.mjs` 抽出）：迷你 GAS 模擬器，`vm` 入面**真正執行** `Code.gs`。
* `tests/_realgas.mjs`（**新**）：把「真 Code.gs」包做一個 HTTP `/exec`（另有 `/_rows` 睇分頁行結構、
  `/_clean` 叫逃生門）。
* `tests/e2ereal.mjs`（**新**，37 項）：真前端（jsdom）→ 真 `api/proxy.js`（dev-server）→ 真 `Code.gs`。
  包括：小 db 換機讀返；**3.25 MB 分件儲存 → 後端真係收到晒 1602 個團員 → 另一部機分段讀返晒**；
  **連續三次分件儲存之後分頁行數仍然 ＝ 正式段數（唔會越存越大）**；`dbInfo` 報垃圾行數；
  逃生門清得走而正式資料唔甩；「同步診斷」全綠。
  *呢個檔喺修之前會紅燈（「分件暫存行已清走」失敗，留低 75 行垃圾）。*
* `tests/remote.mjs` —— 「寫入前會刪走舊段」由 grep 原始碼升級做**真行 Code.gs** 驗行為
  （存 5 段 → 帶 `baseVersion` 再存 1 段 → 分頁只剩 1 段、讀返係最新嗰份）。

```
npm test        # 全綠（含新 tests/e2ereal.mjs）
npm run lint    # 14 / 0
```

---

# 同日再後（前端跟進；後端仍然 v2.6.2）：「話已寫入，但張 Sheet 完全冇嘢」—— 用戶角度完整模擬搵到嘅真相

> 團長原話：「還是不行，他說寫入後端還是完全沒有。幫我以用戶角度完整模擬整個過程看看
> 有沒有什麼發現，究竟是什麼問題導致寫不入，但又會說自己已寫入，如果以寫入寫了到那？
> 又為什麼讀不到。」

## 點樣查：真係開個 app 行一次（`tests/usersim.mjs`）

唔再由 `remote.js` 入手，而係用 jsdom **真正載入 `assets/js/main.js`**、行真 HTTP：
開網站 → 撳「領袖」門 → 打 `leader`／`8202` → 撳「進入系統」→ 加團員＋一筆帳 →
撳頂部**「儲存到後端（2）」** → 記低用戶睇到嘅 toast／狀態 chip →
再直接問後端同開成個 Spreadsheet 睇。後端係**真 Code.gs**（vm 執行）。

## 模擬結果：寫入根本就成功咗

| 步驟 | 用戶／系統睇到 | 後端實況 |
|---|---|---|
| 開機 | 登入頁（唔係連線閘）＝後端答到 | `status` v2.6.2 |
| 撳「儲存到後端（2）」 | 狀態 chip 變「**已存到後端**」；同步紀錄「✓ 已儲存到後端（2 KB）」 | `dbInfo`：`found=true, bytes=1879, members=1, transactions=1` |
| 直接 `loadDb` | — | 團員名 `["陳大文"]` ✓ |
| 另一部全新機（＝無痕視窗）登入 | 用戶頁**睇到「陳大文」** ✓ | — |

**所以「寫唔入」唔成立 —— 資料真係入咗後端，另一部機都讀得到。**

## 咁「完全冇嘢」係點嚟？—— 開張 Sheet 睇嘅話，真係一片空白

撳完「儲存到後端」之後，成個 Spreadsheet 17 張分頁嘅行數：

```
資料庫    1 行   ← ["0082","1","{\"schema\":2,\"kind\":\"real\",…"]（一段 JSON，人睇唔明）
帳目      0 行
團員      0 行
物資／通告／會議／收支申報／物資借用／報名／進度追蹤 …  全部 0 行
```

死因係**設計**，唔係故障：

* 「**儲存到後端**」（`saveToBackend` → `saveDb`）淨係寫**「資料庫」分頁**
  （成份 app 資料序列化成 JSON，每 45000 字一段，第一欄係旅團編號）——
  呢個先係 app 讀返嘅正本，但**人喺 Sheet 度睇唔明**。
* 團員／帳目／物資…嗰啲**攤平報表分頁**要撳另一粒掣「**更新報表分頁**」
  （`pushToMaster`，帶 `skipDb`，唔會掂資料庫）先會填。

所以團長撳完「儲存到後端」→ 開 Google Sheet → 見到 16 張分頁全空 ＋ 一行亂碼 →
結論自然係「佢話已寫入，但後端完全冇嘢」。模擬入面撳多一粒「更新報表分頁」，
`團員(1)`、`帳目(1)` 即刻出現 —— 證明資料一路都喺度。

## 「讀唔到」嘅兩個真死因（同上面嗰個唔同）

1. **你開嘅 Sheet 唔係平台登記嗰張。** 寫入去邊張表由 Vercel 環境變數
   `TROOP_0082_BACKEND` 決定；前端「同步設定」貼咗自己個 `/exec` 都**唔會**用到
   （代理優先，只有平台未登記先會跌落自助路線）。如果登記咗嘅係另一張表
   （例如舊測試表），app 讀寫一切正常，但你開自己嗰張就係空白。
   → 而家「**同步診斷**」會顯示後端自報嘅**試算表名**（`status.spreadsheet`），一撳就知。
2. **舊版垃圾行塞滿「資料庫」分頁**（v2.6.1 之前嘅 `cleanStaging` 漏刪，見上一節）
   → 讀寫一齊撞 GAS 上限。貼 v2.6.2 Code.gs ＋ 執行 `cleanStaleStaging()` 清走。

順帶查到一個陷阱（未修，先記低）：用 `?u=82`（少咗前導零）開，
平台 Registry 認唔到（`TROOP_0082_*` 對唔上 `82`）→ 硬閘直接擋住登入
（「未能連接旅團後端」）。好消息係佢**唔會**靜靜地寫去另一個「旅團」——
模擬已釘死呢一點（`資料庫` 分頁嘅旅團欄永远係 `0082`）。

## 修咗乜（全部係前端；`Code.gs` 唔使再更新，仍然 v2.6.2）

* `assets/js/views/syncdialog.js` —— **一粒掣做齊兩處**：`saveWithDialog()` 儲存成功之後
  自動 `pushToMaster({silent:true})` 刷新報表分頁（仍然只有一條 **db** 寫入路：
  `pushToMaster` 帶 `skipDb`，唔會碰「資料庫」分頁，所以「只有一個儲存方式」嘅定案冇被破壞）。
* 同檔 `saveResultText()` —— 成功訊息而家會**講明寫咗去邊**：
  `已儲存到後端 ✓（「資料庫」分頁 2 KB · 報表分頁已更新（2 筆，團員／帳目…嗰啲分頁而家有嘢睇））`；
  報表刷新失敗會如實講「資料本身已經存到」＋去邊度補撳。
* `assets/js/lib/remote.js` —— 「同步診斷」嘅「後端回應」格加埋**寫入緊嘅試算表名**，
  並提示「如果你開緊嘅 Sheet 唔係呢個名 ＝ 平台登記咗另一張表」。
* `assets/js/views/tables.js` —— `pushToMaster()` 回埋 `total`（今次攤平幾多筆）。
* `tests/usersim.mjs`（**新**，19 項）—— 上述個用戶角度模擬，釘死：
  ① 開機出到登入頁（硬閘通過）② 登入到主控頁 ③ 撳儲存後 toast 講明寫咗去邊
  ④ 後端真係收到 ⑤ **撳完儲存，「團員」／「帳目」分頁即刻有嘢（唔使撳第二粒掣）**
  ⑥ 無痕視窗讀得返 ⑦ `?u=82` 唔會靜靜地寫去另一個旅團。
  *修之前 ⑤ 係紅燈（16 張分頁全空）。*

```
npm run lint        # 14 / 0
npm test            # 全綠（新 usersim 19/19、e2ereal 37/37、gas 120/120、remote 251/251）
```
