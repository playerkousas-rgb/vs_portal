/* ============================================================
   version.js — 前端版號（single source of truth）
   ------------------------------------------------------------
   登入閘／收據／後端實況顯示嘅「APP 版本」就係呢度。
   ★ 一定要同 assets/js/lib/gastemplate.js 嘅 BACKEND_VERSION 一致
     （scripts/lint.mjs 會擋住唔一致 —— 前後端同一個 repo 同步發版）。
   ============================================================ */
export const APP_VERSION = 'v2.8.4';
